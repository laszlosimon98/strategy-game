import { canvasHeight, canvasWidth } from "../../../init";

export const inputPos = {
  auth: {
    name: {},
    password: {},
  },
  code: {
    x: canvasWidth / 2 - 375,
    y: canvasHeight / 2 - 20,
  },
};
