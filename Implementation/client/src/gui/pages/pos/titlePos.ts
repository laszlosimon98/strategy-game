import { canvasHeight, canvasWidth } from "../../../init";
import { TITLE_SIZE } from "../../../settings";

export const titlePos = {
  x: canvasWidth / 2 - TITLE_SIZE.width / 2,
  y: canvasHeight / 15,
};
